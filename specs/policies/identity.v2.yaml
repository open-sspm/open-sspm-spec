kind: opensspm.entity_policy_pack
schema_version: 2
entity_policy_pack:
  metadata:
    id: builtin.identity.risk
    version: 1.0.0
    domain: identity
  inputs:
    schema: identity_risk_input.v1
  policy:
    engine: rego
    package: opensspm.entity.identity_risk
    query: data.opensspm.entity.identity_risk.result
    rego_path: identity.v2.rego
