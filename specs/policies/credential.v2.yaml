kind: opensspm.entity_policy_pack
schema_version: 2
entity_policy_pack:
  metadata:
    id: builtin.credential.risk
    version: 1.0.0
    domain: credential
  inputs:
    schema: credential_risk_input.v1
  policy:
    engine: rego
    package: opensspm.entity.credential_risk
    query: data.opensspm.entity.credential_risk.result
    rego_path: credential.v2.rego
