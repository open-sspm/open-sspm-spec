{
  "schema_version": 1,
  "kind": "opensspm.test_case",
  "rule_key": "OKTA-APP-000020",
  "description": "Should fail when maxSessionIdleMinutes is 30",
  "inputs": {
    "okta:policies/sign-on": [
      {
        "policy": { "name": "Default Policy" },
        "priority": 1,
        "name": "Custom Rule",
        "actions": { "signon": { "session": { "maxSessionIdleMinutes": 30 } } }
      }
    ]
  },
  "expect": "fail"
}
